Generators for special targets which don't really fit into the main project.\
To run these, you need to open the project in an IDE and run the main class of the generator.\
These generators do not have a proper config file, you might need to modify the code to change parameters and make them work with your setup.
